# No 'user' directive — nginx is started by ubuntu, not root, in this test container.
worker_processes auto;
pid /run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    # Launchpad writes one file per service here; nginx picks them up on reload.
    include /etc/nginx/launchpad/*.conf;
}
