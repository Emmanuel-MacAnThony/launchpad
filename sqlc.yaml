version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries/services.sql"
    schema: "db/migrations/"
    gen:
      go:
        package: "infra"
        out: "internal/service/infra"
        sql_package: "pgx/v5"
        emit_json_tags: false
        emit_interface: false
